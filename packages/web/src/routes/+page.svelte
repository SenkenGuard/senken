<!--
	This route always redirects to `/dashboard` before anything would ever
	render here (see `+page.ts`) — the server-backed dashboard, with
	workspaces a user actually owns, is the one that counts. This file is
	kept empty rather than removed: SvelteKit still needs a `+page.svelte`
	alongside `+page.ts` for the route to build.
-->
