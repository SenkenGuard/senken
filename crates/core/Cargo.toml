[package]
name = "senken-core"
description = "Domain-agnostic primitives shared across Senken — one time type, fixed-point scaled integers, and filesystem path encoding — with no I/O of its own"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
categories.workspace = true
keywords.workspace = true
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
# Civil-datetime-plus-zone to instant conversion (`zone`/`time` modules).
# Configured workspace-wide with a bundled tzdb and no system time zone
# access — see the workspace `Cargo.toml` entry for why.
jiff = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
